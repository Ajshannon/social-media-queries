-- CREATE TABLE posts (
--     id serial  PRIMARY KEY,
--     title varchar(50),
--     body varchar(500),
--     user_id INTEGER,
-- )

INSERT INTO posts
(title, body, user_id)
VALUES ('Title announcement', 'The API is awesome!', 1 );
INSERT INTO posts
(title, body, user_id)
VALUES ('Just a test', 'This is just a test. I repeat, this is just a test.', 1 )